language: node_js

node_js:
  - "8"

cache:
  directories:
  - node_modules

before_script:
  - ./startGanache.sh

script:
  - npm run solium:lint
  - truffle test --development
  - ls -a
  - npm run build

after_script:
  - ./stopGanache.sh

deploy:
  provider: heroku
  email: "dev@cipherz.com"
  api_key:
    secure: $HEROKU_API_KEY
  app: "jedibet"
  skip_cleanup: true
  on:
    branch: master